<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lff.com.ar.free_fire">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".Contacto"
            android:label="@string/title_activity_contacto"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Registro"
            android:label="@string/title_activity_registro"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BarraLateral"
            android:label="@string/title_activity_barra_lateral"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".armas"
            android:label="@string/title_activity_armas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".personajes"
            android:label="@string/title_activity_personajes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".clasificatorias"
            android:label="@string/title_activity_clasificatorias"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".mapas"
            android:label="@string/title_activity_mapas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".balas"
            android:label="@string/title_activity_balas"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>